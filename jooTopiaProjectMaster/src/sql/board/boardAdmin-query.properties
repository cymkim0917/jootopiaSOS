insertAdminNotice=INSERT INTO BOARD(BNO,BID,BTYPE, BTITLE, BCONTENT, UNO) VALUES(1,1,1,?,?,1)
noticeListCount=SELECT COUNT(*) FROM BOARD WHERE BTYPE=1 AND STATUS='N'
selectNoticeListPaging=SELECT X.RNUM, X.BID, X.BTITLE, X.ENROLL_DATE, X.MODIFY_DATE, X.BCOUNT, X.BTYPE FROM(SELECT ROWNUM AS RNUM, A.BID, A.BTITLE, A.ENROLL_DATE, A.MODIFY_DATE, A.BCOUNT, A.BTYPE FROM (SELECT ROWNUM AS RNUM, BID, BTITLE, ENROLL_DATE, MODIFY_DATE, BCOUNT, BTYPE FROM BOARD ORDER BY BID DESC) A)X WHERE X.RNUM BETWEEN ? AND ?
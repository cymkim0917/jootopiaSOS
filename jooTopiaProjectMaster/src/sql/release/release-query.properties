selectList=SELECT D.STATUS, P.POID, D.DID, PP.PID, H.HID, H.LBARCODE FROM PORDER P JOIN DELIVERY D ON (P.POID = D.POID) JOIN ORDER_DETAIL O ON (D.POID = O.POID) JOIN HEAP H ON (O.PID = H.PID) JOIN PRODUCT PP ON (H.PID = PP.PID) JOIN RELEASE R ON (PP.PID = R.PID) WHERE D.STATUS = ?
selectReleaseList=SELECT P.POID, D.DID, R.RLID, R.RLDATE FROM PORDER P, DELIVERY D, RELEASE R WHERE P.POID = D.POID AND D.POID = R.POID
selectOneReleaseAdmin=SELECT * FROM PORDER P, ORDER_DETAIL OD, DELIVERY D, HEAP H, ATTACHMENT A WHERE P.POID = OD.POID AND OD.POID = D.POID AND OD.PID = H.PID AND H.PID = A.PID AND P.STATUS = '\uC8FC\uBB38\uC644\uB8CC' AND A.FILE_LEVEL = 0 AND D.DID = ?
insertAdminRelease=INSERT INTO RELEASE VALUES (SEQ_RLID.NEXTVAL, DEFAULT, DEFAULT, ?, ?)
selectListAfterPaging=SELECT RNUM, PID, HID, RLID, PCDID, CGROUP, NAME, POID, DID, LBARCODE, PCONTENT, RLDATE FROM (SELECT ROWNUM RNUM, PID, HID, RLID, PCDID, CGROUP, NAME, POID, DID, LBARCODE, PCONTENT, RLDATE FROM (SELECT P.PID, H.HID, R.RLID, PD.PCDID, C.CGROUP, C.NAME, PO.POID, D.DID, H.LBARCODE, P.PCONTENT, R.RLDATE FROM PRODUCT P, HEAP H, RELEASE R, PURCHASE_DETAIL PD, CATEGORY C, PORDER PO, DELIVERY D WHERE P.PID = H.PID(+) AND H.PID = R.PID AND R.POID = PO.POID AND PO.POID = D.POID AND P.CID = C.CID AND P.PCDID = PD.PCDID ORDER BY P.PID DESC)) WHERE RNUM BETWEEN ? AND ?
listCount=SELECT COUNT(*) FROM PORDER P, DELIVERY D, RELEASE R WHERE P.POID = D.POID AND D.POID = R.POID
selectList=SELECT * FROM PURCHASE_DETAIL PCD JOIN PURCHASE PC ON (PCD.PCID = PC.PCID) LEFT JOIN PRODUCT P ON(P.PCDID = PCD.PCDID) JOIN CATEGORY C ON(C.CID = PC.CID) WHERE PCD.STATUS = ? AND P.PID IS NULL ORDER BY PCD.PCDID DESC